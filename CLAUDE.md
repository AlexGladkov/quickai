# quickai

Профайлер работы Claude Code. См. ARCHITECTURE.md и README.md.

DESIGN_SYSTEM: notion

HTML-отчёт (`src/report.rs`) оформлен по Notion, **тёмная тема по умолчанию**:
тёплый тёмный холст `#191919`, Inter с отрицательным трекингом, whisper-борды
`1px rgba(255,255,255,.09)`, синий accent, мягкие тени, метрики крупным числом (700).
Заголовок: **имя проекта крупно и ярко** (56px), `quickai` — вторичный eyebrow.
Семантика цветом: err%/idle% высокий → orange/red, cache-hit высокий → teal.
